# Perfect Model

## Write-up

- The server uses pickle to load the model.
- Craft a malicious model that spawns a shell.
- Once inside the container you can find the private.csv dataset.
- Make a model now using the private data to get 100% accuracy score.

[Exploit script](./exploit.py).
